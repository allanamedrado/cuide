<form method="post" action="{{ path('sintoma_delete', {'sintomaId': sintoma.sintomaId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sintoma.sintomaId) }}">
    <button class="btn">Delete</button>
</form>
