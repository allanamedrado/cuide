{% extends 'base.html.twig' %}

{% block title %}Exame{% endblock %}

{% block body %}
    <h1>Exame</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ExameId</th>
                <td>{{ exame.exameId }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ exame.nome }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ exame.data }}</td>
            </tr>
            <tr>
                <th>Local</th>
                <td>{{ exame.local }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exame_index') }}">back to list</a>

    <a href="{{ path('exame_edit', {'exameId': exame.exameId}) }}">edit</a>

    {{ include('exame/_delete_form.html.twig') }}
{% endblock %}
