<form method="post" action="{{ path('medicamento_delete', {'medicamentoId': medicamento.medicamentoId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medicamento.medicamentoId) }}">
    <button class="btn">Delete</button>
</form>
