{% extends 'base.html.twig' %}

{% block title %}Consulta{% endblock %}

{% block body %}
    <h1>Consulta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ConsultaId</th>
                <td>{{ consultum.consultaId }}</td>
            </tr>
            <tr>
                <th>Medico</th>
                <td>{{ consultum.medico }}</td>
            </tr>
            <tr>
                <th>Especialidade</th>
                <td>{{ consultum.especialidade }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ consultum.data ? consultum.data|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Hora</th>
                <td>{{ consultum.hora }}</td>
            </tr>
            <tr>
                <th>Local</th>
                <td>{{ consultum.local }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('consulta_index') }}">back to list</a>

    <a href="{{ path('consulta_edit', {'consultaId': consultum.consultaId}) }}">edit</a>

    {{ include('consulta/_delete_form.html.twig') }}
{% endblock %}
